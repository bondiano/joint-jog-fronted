# Joint jog app frontend repository

## Step to run locally

- Clone this repo `git clone https://github.com/bondiano/joint-jog-fronted`
- Run `npm install` or `yarn`
- Run `npm start` or `yarn start`